#include "main.h"


/**
 * _abs - computes the absolute value of an integer
 * @n: n is an integer
 *
 * Retrun: integer
 */

int _abs(int i)
{

	if (i > 0)
	{
	return (i);
	}
	else if (i < 0)
	{
	return(-i);
	}
	else
	{
	return(i);
	}
}
